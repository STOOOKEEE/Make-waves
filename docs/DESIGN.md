# DESIGN.md — obsolète

⚠️ Ce fichier était une **copie bit-à-bit** de [`BRAND.md`](./BRAND.md), ce qui
donnait deux sources de vérité pour le même design system et garantissait qu'à
la première modification, l'une des deux mentirait.

**La source de vérité du design system est [`BRAND.md`](./BRAND.md).** C'est ce
que référencent `apps/web/src/styles/tokens.css` et `base.css`.

Ce stub est conservé pour ne pas casser les liens historiques du `DEVLOG` et des
specs datées. Ne rien y ajouter.
